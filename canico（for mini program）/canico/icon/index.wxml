<view
  class="custom-class {{ classPrefix }} {{ classPrefix }}-{{ name }}"
  style="{{ color ? 'color: ' + color + ';' : '' }}{{ size ? 'font-size: ' + size + ';' : '' }}{{ customStyle }}"
  bind:tap="onClick"
>
  <view wx:if="{{ info !== null }}" class="cd__info">{{ info }}</view>
</view>
